machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t glio -f dev/setup/Dockerfile .

deployment:
  production:
    branch: master
    heroku:
      appname: gliderlabs-io
